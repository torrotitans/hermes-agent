# Execution Plan: Layer 5 - Memory (Vectorized Graph Thinking)

## System Context for 7B Model
**Role:** You are the execution agent tasked with building Layer 5 of the Torro Agent Architecture.
**Goal:** Implement the state continuity layer utilizing pgvector and Apache AGE for associative reasoning.
**Invariants to Enforce:** 
- NO RAW SQL: Must use `SQLModel` for schema definitions and queries (except where Apache AGE Cypher integration mandates explicit wrapping).
- `Relationship()` fields MUST be defined on `table=True` models, NOT Base models.
- Strict Type Safety throughout.

---

## Phase 1: Database Initialization & Schema Design
**Token Estimate:** ~1M Tokens
**Objective:** Set up the SQLModel schemas and integrate pgvector.

### Task 1.1: Vector Storage Setup
**Token Budget:** 150k Tokens | **Time Estimate:** 5-10 Minutes
**Inputs Needed:** `engine/memory/models.py` (new)
**Action Steps:**
1. Create `engine/memory/models.py`.
2. Define SQLModel schemas for `AgenticExperience` and `AgenticPlan`.
3. Use the `pgvector` python library `Vector` type for a `semantic_embedding` field on `AgenticExperience`.
4. Enforce the invariant: Do not put SQLModel relationships on the base class.
**Acceptance Criteria:** `models.py` executes without `TypeError` and properly imports `pgvector` types.

### Task 1.2: Graph Edge Schema (Mock Apache AGE)
**Token Budget:** 150k Tokens | **Time Estimate:** 5-10 Minutes
**Inputs Needed:** `engine/memory/models.py`
**Action Steps:**
1. In `models.py`, define a `LogicNode` and `LogicEdge` SQLModel class.
2. `LogicNode` represents a decision or task.
3. `LogicEdge` represents the traversal (e.g., "Led To", "Failed Because").
4. This acts as the relational fallback before writing direct Cypher queries.
**Acceptance Criteria:** The graph edge schema successfully establishes many-to-many relationships in SQLModel.

---

## Phase 2: Cognitive Retrieval & Consolidation
**Token Estimate:** ~1M Tokens
**Objective:** Build the memory retrieval logic and background consolidation.

### Task 2.1: Semantic Similarity Search
**Token Budget:** 150k Tokens | **Time Estimate:** 5-10 Minutes
**Inputs Needed:** `engine/memory/retrieval.py` (new), `engine/memory/models.py`
**Action Steps:**
1. Create `engine/memory/retrieval.py`.
2. Define `MemoryEngine` class with `FN:semantic_search`.
3. Implement a mock query that filters `AgenticExperience` by `cosine_distance`.
**Acceptance Criteria:** The query syntax aligns with SQLModel/pgvector patterns (e.g., `session.exec(select(AgenticExperience).order_by(AgenticExperience.semantic_embedding.cosine_distance(query_vec))).first()`).

### Task 2.2: Context Condensing (autoDream)
**Token Budget:** 150k Tokens | **Time Estimate:** 5-10 Minutes
**Inputs Needed:** `engine/memory/consolidation.py` (new)
**Action Steps:**
1. Create `engine/memory/consolidation.py`.
2. Define `ConsolidationWorker` with `FN:condense_trajectory`.
3. Accept an array of mock `LogicNode` objects representing a past execution path.
4. Summarize their text and output a single, merged `LogicNode`.
**Acceptance Criteria:** The function reduces a list of N nodes down to 1 summarized node, simulating the pruning of token-heavy historical contexts.
